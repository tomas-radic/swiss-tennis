<%= form_with(model: [:manager, season], local: true) do |form| %>

  <div class="row">
    <div class="col">
      <div class="<%= add_error_class_to('pl-2', @season, :name) %>">
        <div class="field form-group">
          <%= form.label :name, 'Názov' %><br />
          <%= form.text_field :name, autofocus: true, class: 'form-control' %>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <div class="actions">
        <%= submit_tag 'Potvrdiť', class: 'btn btn-lg btn-success my-1' %>
        <%= link_to 'Zrušiť', manager_seasons_path, class: 'btn btn-lg btn-outline-success my-1' %>
      </div>
    </div>
  </div>
  <%= hidden_field_tag :heading, @heading %>
<% end %>
